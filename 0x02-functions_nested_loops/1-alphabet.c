#include "main.h"
/**
 * print_alphabet - this is the function we are using
 * return: always 0
 */
void print_alphabet(void)
{
char b;
for (b = 'a'; b <= 'z'; b++)
{
_putchar(b);
}
_putchar('\n');
}
