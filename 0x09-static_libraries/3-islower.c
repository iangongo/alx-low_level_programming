#include "main.h"
/**
 * _islower - function name checks for lowercase character
 * @c: function parameter the character to check
 * Return: 1 if c is lowercase, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
