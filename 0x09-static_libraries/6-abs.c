#include "main.h"

/**
 * _abs - function name computes the absolute value of an integer
 * @n: parameter one the int to check
 * Return: the absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
	return (n);
	}
	return (-n);
}
